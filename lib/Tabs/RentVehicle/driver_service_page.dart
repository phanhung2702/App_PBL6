import 'package:flutter/material.dart';

class DriverServicePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Thuê Xe Có Tài Xế'),
      ),
      body: const Center(
        child: Text('Nội dung cho trang Thuê Xe Có Tài Xế.'),
      ),
    );
  }
}